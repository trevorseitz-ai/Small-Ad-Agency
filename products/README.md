# Products Directory

This directory stores all active and generated publishing products for the agency.

## Structure
- Each guide, workbook, or survival manual will have its own dedicated subdirectory under this folder.
- Subdirectories will follow the format: `products/<product-id>-<product-slug>/`.
- The product subdirectories will copy the template structure defined in `templates/product-template/`.
